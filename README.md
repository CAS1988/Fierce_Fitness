# Tiger v3.5.4

Fresh GitHub Pages repository package.

The deployed application files are located directly in the repository root. The
included GitHub Actions workflow publishes the static site to GitHub Pages.

Before installing the phone app, verify `version.txt` and the version shown at
the bottom of Tiger Settings.
